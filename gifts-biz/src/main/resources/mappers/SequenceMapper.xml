<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pandaria.farmland.biz.mapper.SequenceMapper">

    <select id="getIdFor" statementType="PREPARED" resultType="int">
        select nextval(#{tableName,jdbcType=VARCHAR})
    </select>

</mapper>